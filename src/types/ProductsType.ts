export interface ProductsType {
    img: string,
    category: string
    link: string,
    title: string,
    price: number,
    rate: number,
    sale_price?: number,
    badge?: string,
    quantity?: number,
}